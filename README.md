# Fake-Job-Detection
Machine learning system that detects fake job postings using NLP (TF-IDF) and classifiers (Logistic Regression, Naive Bayes, Random Forest). Includes a prediction system for classifying new job postings as legitimate or fraudulent.
